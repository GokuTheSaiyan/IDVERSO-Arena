Place your custom font here:

fonts/main.woff

Supported formats:
* .woff

The game will automatically load main.woff if present.
It will be applied to the game title, menus, character selection, 
countdown text, health bars, resource bars, battle log, and victory screens.

If no custom font is present, the default game font (Arial) will be used.